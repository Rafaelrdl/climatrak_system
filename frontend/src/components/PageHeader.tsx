/**
 * @deprecated Use '@/shared/ui' instead
 * Re-export para compatibilidade - será removido em versão futura
 */
export { PageHeader, type PageHeaderProps, type BreadcrumbItem } from '@/shared/ui';
