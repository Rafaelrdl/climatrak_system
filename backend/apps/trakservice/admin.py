"""
TrakService Admin Configuration

Admin interfaces for TrakService models.
"""

from django.contrib import admin

# Register models as they are implemented
# @admin.register(ServiceJob)
# class ServiceJobAdmin(admin.ModelAdmin):
#     list_display = ['id', 'title', 'status', 'assigned_to', 'scheduled_date']
#     list_filter = ['status', 'scheduled_date']
#     search_fields = ['title', 'description']
